# Reference Captures

This folder stores non-runtime screenshot references for the legacy English prototype.

- These `.png` files are not loaded by the current HTML/CSS/JS pages.
- Runtime files stay one level above this folder.
- Keep migration and visual comparison assets here so the live prototype root stays readable.

